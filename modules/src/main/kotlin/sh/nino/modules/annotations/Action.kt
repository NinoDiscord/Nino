package sh.nino.modules.annotations

/**
 * Represents the first action that is executed when the module is being registered
 * in the registry.
 */
@Target(AnnotationTarget.FUNCTION)
annotation class Action
